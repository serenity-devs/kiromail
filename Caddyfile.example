{$APP_DOMAIN} {
  encode zstd gzip
  reverse_proxy app:3000
  header {
    -Server
    Strict-Transport-Security "max-age=31536000; includeSubDomains"
  }
  log {
    output stdout
    format json
  }
}
